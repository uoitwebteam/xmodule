<?php

namespace XModule\Constants;

use \XModule\Constants\Enum;

abstract class AnchorPosition extends Enum
{
  const DEFAULT = 'default';
  const TOP = 'top';
  const BOTTOM = 'bottom';
  const LEFT = 'left';
  const CENTER = 'center';
  const RIGHT = 'right';
}

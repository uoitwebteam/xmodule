<?php

namespace XModule\Constants;

use \XModule\Constants\Enum;

abstract class Position extends Enum
{
  const LEFT = 'left';
  const RIGHT = 'right';
  const MIDDLE = 'middle';
  const CENTER = 'center';
}
